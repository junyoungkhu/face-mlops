name: CI/CD Pipeline - Docker Build and Push

on:
  push:
    branches: [ "main", "master" ]
    paths:
      - 'shuttle_api/**'
      - '.github/workflows/ci.yml'
  pull_request:
    branches: [ "main", "master" ]
    paths:
      - 'shuttle_api/**'

jobs:
  docker-build-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          # Github Actions의 빌드 캐시를 활용하기 위한 Buildx 엔진 설정
          driver-opts: image=moby/buildkit:master

      - name: Login to Docker Hub
        # PR(풀 리퀘스트) 이벤트가 아닐 때만 로그인 수행
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract Metadata (Tags, Labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/shuttle-api
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=short

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          # 리포지토리 루트에서 /shuttle_api 를 Build Context로 지정
          context: ./shuttle_api
          file: ./shuttle_api/Dockerfile
          # PR이 아닌 경우에만 이미지를 Docker Hub에 Push
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # cache-from, cache-to를 통해 GitHub Actions 캐시를 Docker 빌드 캐시로 활용하여 빌드 속도를 획기적으로 개선합니다
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to Local Environment
    needs: docker-build-push
    # PR이 아닌 push 동작 시에만 배포 진행
    if: github.event_name != 'pull_request'
    # GitHub 사이트에서 Local PC를 self-hosted runner로 등록했다고 가정
    runs-on: self-hosted

    steps:
      - name: Debug Environment
        shell: cmd
        run: |
          where docker
          docker version

      - name: Login to Docker Hub
        # PR(풀 리퀘스트) 이벤트가 아닐 때만 로그인 수행
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Pull Latest Docker Image
        shell: cmd
        run: docker pull %DOCKERHUB_USERNAME%/shuttle-api:latest
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}

      - name: Stop Existing Container
        shell: cmd
        continue-on-error: true
        run: docker stop shuttle-api-container

      - name: Remove Existing Container
        shell: cmd
        continue-on-error: true
        run: docker rm shuttle-api-container

      - name: Run New Container
        shell: cmd
        run: docker run -d --name shuttle-api-container -p 8000:8000 --restart unless-stopped %DOCKERHUB_USERNAME%/shuttle-api:latest
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
